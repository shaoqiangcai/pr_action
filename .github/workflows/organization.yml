name: organization

on: push

jobs:
  organization-member:
    runs-on: ubuntu-latest
    steps:
      - name: Check organization
        id: is_organization_member
        run: |
         org="OperationsYU"
         username="fish0607"
         resutl=`curl -I  -H "Accept: application/vnd.github.v3+json" https://api.github.com/orgs/${org}/members/${username}`
         echo ${result}
         username2="fish9167"
         resutl=`curl -I  -H "Accept: application/vnd.github.v3+json" https://api.github.com/orgs/${org}/members/${username2}`
         echo ${result}         
         
